services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persist the SQLite file across container restarts.
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
